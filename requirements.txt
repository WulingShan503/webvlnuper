# 运行时依赖：训练与评测所需。
# 论文实验环境：Python 3.8 / PyTorch 1.13 / CUDA 11.7 / RTX 3090。
#
# 仅运行测试无需安装本文件，见 requirements-dev.txt——
# 单元测试刻意不依赖 torch 与 API key，几秒即可跑完。

torch==1.13.1
torchvision==0.14.1
transformers==4.26.1
numpy==1.23.5
Pillow==9.4.0
PyYAML==6.0
tqdm==4.64.1

# 第四章：LLM 语义排序
openai==0.27.8

# 第五章：WUPS 依赖 WordNet 词义相似度
nltk==3.8.1
